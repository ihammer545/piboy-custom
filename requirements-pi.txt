-r requirements-dev.txt
# The following requirements are only used by the hardware modules for Raspberry Pi.
spidev >= 3.5
rpi-lgpio >= 0.6.0
RPi.bme280 >= 0.2.4
evdev >= 1.7.1
# GPU CRT path (driver: gles) — also needs system SDL2/EGL/GLES packages; see docs/CRT.md
pygame >= 2.5.0
PyOpenGL >= 3.1.0
